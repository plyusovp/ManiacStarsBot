// Здесь будет вся логика, связанная с игрой "Кости"

// `export` делает эту функцию доступной для других файлов
export function initDiceGame() {
    console.log('Dice game logic initialized');
    // Сюда переносится весь код из функции initDiceGame из старого main.js
    // Например:
    // const diceButton = document.querySelector('#dice-button');
    // diceButton.addEventListener('click', () => { ... });
}
