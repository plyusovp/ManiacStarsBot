// --- ИЗМЕНЕНО ---
// Применяем тот же паттерн, что и для других экранов.

export function init(app) {
    app.innerHTML = `
        <h1>Referrals</h1>
        <p>Invite your friends and earn rewards.</p>
    `;
}
